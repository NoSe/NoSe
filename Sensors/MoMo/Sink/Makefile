COMPONENT=MoMoSinkAppC

CFLAGS += -I$(TOSDIR)/../MoMo/interfaces/
CFLAGS += -I$(TOSDIR)/../MoMo/include/
CFLAGS += -I$(TOSDIR)/../MoMo/Application/
CFLAGS += -I$(TOSDIR)/../MoMo/MAC/
CFLAGS += -I$(TOSDIR)/../MoMo/LL/
CFLAGS += -I$(TOSDIR)/../MoMo/Queue/
CFLAGS += -I$(TOSDIR)/../MoMo/Arbiter/
CFLAGS += -I$(TOSDIR)/../MoMo/Multiplexer/

CFLAGS += -DCC2420_NO_ACKNOWLEDGEMENTS
CFLAGS += -DCC2420_NO_ADDRESS_RECOGNITION
CFLAGS += -DLOW_POWER_LISTENING
CFLAGS += -DCC2420_DEF_CHANNEL=23
PFLAGS += -DMM_POLLING_TIME=20

PFLAGS += -DMM_GET_FROM_SENSOR=1
PFLAGS += -DMM_SAMPLING_TIME=9
PFLAGS += -DMM_COLLECTING_TIME=60
PFLAGS += -DMM_MAX_NUM_NEIGH=10

PFLAGS += -DMM_SIGNAL_DUTY
#PFLAGS += -DMM_DEBUG

include $(MAKERULES)
